# Frontend (SvelteKit) public env
# Copy to frontend/.env — must use the same hostname you open in the browser
# (prefer localhost). Must match the uvicorn port.

PUBLIC_API_URL=http://localhost:8001
